define([
        'boilerplate',
    ], 
    function(boilerplate){
        var ConfirmModel = Backbone.Model.extend({
            'initialize': function() {
                //
            }
        });

        return ConfirmModel;
    }
);